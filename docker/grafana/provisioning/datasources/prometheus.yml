apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus.cabal-monitoring.cabal.internal:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 30s
