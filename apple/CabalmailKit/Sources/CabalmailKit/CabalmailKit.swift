import Foundation

/// Namespace for top-level constants exposed by the package.
public enum CabalmailKit {
    /// Marketing version of the package. Mirrors `MARKETING_VERSION` in the app targets.
    public static let version = "0.6.0"
}
