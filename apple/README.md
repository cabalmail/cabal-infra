# Cabalmail Apple Client

Documentation for the native Apple clients lives at
[`docs/apple.md`](../docs/apple.md) — layout, bootstrap, signing,
Developer-account setup, CI, icons, and architecture notes.

(It lives under `docs/` rather than here so that documentation edits do
not trigger the Apple CI build, which runs on any change under `apple/`.)
