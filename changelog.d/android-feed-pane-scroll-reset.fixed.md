- Android: **Feed list keeps its scroll position.** On a tablet, choosing a
  feed or folder in the Feeds tab's leading pane no longer scrolls that pane
  back to the top; it stays where it was until scrolled deliberately.
