- **Android client scaffold (Phase 1).** New `android/` Gradle workspace
  starting the native Android client: `app` module (Jetpack Compose +
  Material 3 shell with dynamic color and platform splash screen) and `kit`
  module (runtime `config.json` fetch/cache service and the placeholder
  `CabalmailClient`), Kotlin-only, min SDK 31, JUnit 5 unit tests, and
  ktlint. See `docs/1.x/android-client-plan.md`.
