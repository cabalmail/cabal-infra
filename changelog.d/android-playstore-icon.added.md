- **Android Play Store icon generated from the brand vector.**
  `make logo` (and the Logo Assets workflow) now also renders
  `android/app/src/main/ic_launcher-playstore.png`, the 512×512 opaque PNG
  the Google Play Console requires for the store listing, from
  `vector/cabalmail-logo.svg` alongside the Apple, React, docs, and
  front-door derivatives.
