- Apple: **macOS app icon now follows the system Appearance setting.** The
  macOS icon shipped only a light size ladder with no appearance variants, so
  it stayed light in Dark mode while the iOS icon switched correctly. It now
  carries a dark variant at every size (rendered from the same source vector),
  which macOS 15+ swaps in when the system is set to Dark.
