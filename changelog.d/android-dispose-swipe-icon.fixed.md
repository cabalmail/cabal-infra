- Android: **Dispose swipe icon matches the action.** The dispose swipe's
  reveal always showed a trashcan; it now shows the archive box unless the
  dispose actually deletes (preference set to Trash, or purging inside
  Trash), matching the reveal's label and the reader's dispose button.
