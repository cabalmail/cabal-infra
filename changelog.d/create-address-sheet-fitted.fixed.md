- **Create Address sheet no longer over-tall on iPad/Mac.** The compose "Create
  Address" form now sizes the sheet to its content (`presentationSizing(.fitted)`)
  instead of filling a fixed-size card, trimming the empty space on iPad and
  macOS. Compact-width iPhone keeps its full-height presentation.
