- Apple: **List and reader split on the crease.** On an iPhone Duo's inner
  display the message or item list takes the left page and the reader the
  right, with the divider on the fold, whether the device is open flat or
  held like a book. The column resize handle is hidden there, since the
  fold is the one divider position that reads right; iPad keeps its
  draggable divider and stored width.
