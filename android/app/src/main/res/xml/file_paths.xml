<?xml version="1.0" encoding="utf-8"?>
<!-- Only the attachment scratch directory is shareable; nothing else in
     the app's storage is ever exposed through the FileProvider. -->
<paths>
    <cache-path
        name="attachments"
        path="attachments/" />
</paths>
